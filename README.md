SimpleScala
===========

A simple console application using scala


To run the application first cd to the source directed. 
Then run 'scalac *.scala' to compile all of the files
Then use 'scala basic.Application'

Enjoy
